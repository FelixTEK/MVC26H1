# MVC26H1
Repo สำหรับการสอบ MVC ในการสอบ [Exit Exam 2/68] การเขียนโปรแกรมแบบ MVC

นี่คือไฟล์ `README.md` ที่เขียนสรุปข้อมูลสำคัญทุกอย่างให้ครบถ้วนและดูเป็นมืออาชีพครับ ก๊อปปี้ไปสร้างไฟล์ใน Root Folder แล้วส่งขึ้น GitHub ได้เลย!

---

# 🗳️ ระบบติดตามคำสัญญานักการเมือง (Political Promise Tracker)

**การสอบ Exit Exam MVC 2/68 (ข้อ 1)**

ระบบนี้พัฒนาขึ้นเพื่อติดตามสถานะคำสัญญาของนักการเมือง โดยใช้สถาปัตยกรรมแบบ **MVC (Model-View-Controller)** แยกส่วนการทำงานชัดเจนตามโจทย์กำหนด

---

### 🛠️ Tech Stack & Environment

* **Stack:** XAMPP
* **Language:** PHP 8.0.30 (VS16 X86 64bit thread safe) + PEAR
* **Database:** MariaDB 10.4.32
* **Server:** Apache 2.4.58
* **DB Frontend:** phpMyAdmin 5.2.1
---

### ⚙️ การติดตั้งและใช้งาน (Installation)

1. **Import Database:**
* สร้างฐานข้อมูลชื่อ `mvc` (Collation: `utf8mb4_thai_520_w2`)
* นำไฟล์ `database.sql` (ใน root folder) ไป Import เข้าฐานข้อมูล


2. **Config:**
* ตรวจสอบไฟล์ `config/Database.php` ให้ username/password ตรงกับเครื่องของท่าน (Default: root/ไม่มี password)


3. **Run:**
* เปิด Browser เข้าไปที่ `http://127.0.0.1/mvc/index.php`


---

### 🔐 การเข้าสู่ระบบผู้ดูแล (Authentication)

ระบบนี้ **ไม่มีการสมัครสมาชิก (No Registration)** เนื่องจากจำลองให้ใช้งานเฉพาะเจ้าหน้าที่ภายใน
* **Username:** `admin`
* **Password:** `1234`

---

### 📂 โครงสร้างโปรเจกต์ (Project Structure)

การทำงานแยกส่วนตามหลักการ MVC ดังนี้:

* **📂 models/ (`Promise.php`)**
* ทำหน้าที่เชื่อมต่อ Database
* ดึงข้อมูลคำสัญญา (`getAll`, `getById`)
* บันทึกการอัปเดตความคืบหน้า (`addUpdate`)


* **📂 views/**
* `promise_list.php`: แสดงรายการคำสัญญาทั้งหมด เรียงตามวันที่ล่าสุด
* `promise_detail.php`: แสดงรายละเอียดและ Timeline ประวัติการทำงาน
* `promise_update.php`: ฟอร์มบันทึกความคืบหน้า (เข้าได้เฉพาะ Admin)
* `politician_detail.php`: แสดงข้อมูลนักการเมืองและคำสัญญาทั้งหมดของเขา
* `login.php`: หน้าจอลงชื่อเข้าใช้งาน


* **📂 controllers/ (`PromiseController.php`)**
* เป็นตัวกลางรับ Request จาก Router (`index.php`)
* **Business Rules Enforcement:**
1. ตรวจสอบสิทธิ์ Admin ก่อนให้เข้าหน้า Update
2. **ตรวจสอบสถานะ:** หากคำสัญญาเป็นสถานะ **"เงียบหาย" (Silent)** จะไม่อนุญาตให้อัปเดตข้อมูล

---

### 🚀 Routes / Actions หลัก

* `index.php?action=index` : หน้าแรก (รายการคำสัญญา)
* `index.php?action=show&id=...` : หน้ารายละเอียด
* `index.php?action=update&id=...` : หน้าฟอร์มอัปเดต (Admin Only)
* `index.php?action=save` : บันทึกข้อมูลลงฐานข้อมูล
* `index.php?action=politician&id=...` : หน้ารวมสัญญาของนักการเมืองแต่ละคน
* `index.php?action=login_check` : ตรวจสอบรหัสผ่าน

---